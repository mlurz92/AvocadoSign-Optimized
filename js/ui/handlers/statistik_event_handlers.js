const statistikEventHandlers = (() => {

    function init() {
        // Event listeners for the "Statistik" tab are primarily handled within statistikTabLogic.js
        // to keep logic and event handling for that specific tab encapsulated.
        // No additional event listeners are needed here at this moment.
    }

    return Object.freeze({
        init
    });

})();
